import type { ProblemSession } from "../../domain/session/ProblemSession";export class TabVisibilityTracker {    private session: ProblemSession;    private hiddenAt: number | null = null;    private started = false;    private readonly handleVisibilityChange =        (): void => {            if (document.hidden) {                this.handleHidden();            } else {                this.handleVisible();            }        };    constructor(session: ProblemSession) {        this.session = session;    }    public start(): void {        if (this.started) {            return;        }        this.started = true;        document.addEventListener(            "visibilitychange",            this.handleVisibilityChange        );    }    public stop(): void {        if (!this.started) {            return;        }        document.removeEventListener(            "visibilitychange",            this.handleVisibilityChange        );        this.started = false;        /*         * If the tracker is stopped while the         * page is hidden, close the current         * away interval.         */        if (this.hiddenAt !== null) {            this.handleVisible();        }    }    private handleHidden(): void {        /*         * Prevent duplicate hidden events.         */        if (this.hiddenAt !== null) {            return;        }        this.hiddenAt = Date.now();        this.session.tabSwitchCount++;        this.session.events.push({            type: "TAB_HIDDEN",            timestamp: this.hiddenAt        });        console.log(            "[DSA Tracker] Tab hidden."        );    }    private handleVisible(): void {        if (this.hiddenAt === null) {            return;        }        const visibleAt = Date.now();        const awayDuration =            visibleAt - this.hiddenAt;        this.session.totalTimeAway +=            awayDuration;        this.session.events.push({            type: "TAB_VISIBLE",            timestamp: visibleAt        });        console.log(            "[DSA Tracker] Tab visible."        );        console.log(            "[DSA Tracker] Time away:",            awayDuration,            "ms"        );        console.log(            "[DSA Tracker] Total time away:",            this.session.totalTimeAway,            "ms"        );        this.hiddenAt = null;    }}