import type { ProblemMetadata } from "../problem/ProblemMetadata";import type { SessionEvent } from "./SessionEvent";export interface ProblemSession {    sessionId: string;    problem: ProblemMetadata;    sessionStartedAt: number;    firstCodingAt: number | null;    solvedAt: number | null;    thinkingDuration: number | null;    codingDuration: number | null;    totalTimeAway: number;    tabSwitchCount: number;    language: string | null;    copyCount: number;    pasteCount: number;    hintOpened: boolean;    hintOpenCount: number;    solutionViewed: boolean;    solutionViewedAt: number | null;    editorialViewed: boolean;    editorialViewedAt: number | null;    attempts: number;    events: SessionEvent[];    solved: boolean;}